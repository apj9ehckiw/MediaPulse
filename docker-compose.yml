services:
  haijiao-web:
    build: .
    image: haijiao-web:latest
    container_name: haijiao-web
    restart: unless-stopped
    ports:
      # 自定义端口：在 .env 里设置 HJ_PORT=9090（或启动时 HJ_PORT=9090 docker compose up -d）
      # 冒号右边是容器内固定端口（8080），左边是宿主机访问端口
      - "${HJ_PORT:-8080}:8080"
    environment:
      # 监听地址（容器内固定 8080，对外端口用 HJ_PORT 调整）
      HJ_ADDR: ":8080"
      # 数据目录（config.json / state.json / downloads.json / videos/）
      HJ_DATA_DIR: "/data"
      # 首次启动的默认轮询间隔秒（之后以网页端「设置」为准）
      HJ_INTERVAL: "600"
      # 首次启动种子访问密码（可选；不设置则首次打开网页走设置向导）
      # HJ_PASSWORD: "your-password"
    volumes:
      - ./data:/data   # videos/ 与 state.json 都落在宿主机 ./data
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/api/status"]
      interval: 30s
      timeout: 5s
      retries: 3
